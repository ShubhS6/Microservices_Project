spring:
  application:
    name: configdemo
  config:
    import: configserver:http://localhost:8888


  profiles:
    active: prod

#build:
#  id: 122
#  version: 2.2.2
#  name: "Default-Build"
#  type: "Default Build"


build:
  id: ${ID}
  version: ${VERSION}
  name: ${NAME}

management:
  endpoints:
    web:
      exposure:
        include: refresh