spring:
  application:
    name: configserver

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  cloud:
    config:
      server:
        git:
          uri: https://github.com/ShubhS6/Microservices_Project_config
#          username: ${GIT_USER}
#          password: ${GIT_TOKEN}

server:
  port: 8888


management:
  endpoints:
    web:
      exposure:
        include: "*"